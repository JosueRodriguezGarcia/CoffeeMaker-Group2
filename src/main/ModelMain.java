package main;

public class ModelMain {

	public static void main(String[] args) {
		
		/* Default state: i.e. no parameter passed, all attributes (objects) values are set to null */
		//CoffeeMaker miCafetera = new CoffeeMaker.Builder().build();

		/* Or you may pass as many parameters as you wish */
		//FilterSensor sensorsillo = new FilterSensor();
		//CoffeeMaker miCafetera = new CoffeeMaker.Builder().sensor(sensorsillo).build();
		/* Or use new ones */
		//CoffeeMaker miCafetera = new CoffeeMaker.Builder().sensor(new FilterSensor()).build();
		
		/* Whether the model is instantiated with or without a given object can also depend on what is set on a GUI panel for example. */
		
	}
}
